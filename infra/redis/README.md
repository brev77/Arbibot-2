# Redis (Phase 1.1-REDIS)

Phase 1 dev: Redis не обязателен. Запланировано для кэша котировок, distributed lock на короткие окна резерва и rate limits.

Подключение: задокументировать политику ключей и TTL при появлении первого use case (например TTL резерва дублируется в БД; Redis — ускорение и координация).
