/** Policy key in config-service; value is JSON — see docs/paper-discovery-config-keys.md */
export const PAPER_DISCOVERY_POLICY_KEY = 'paper.discovery';

export const DEFAULT_PAPER_DISCOVERY_CONFIG_CACHE_MS = 15_000;
