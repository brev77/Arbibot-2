export default function Loading() {
  return (
    <main style={{ padding: '1.5rem 2rem' }}>
      <h1>Loading operator surface</h1>
      <p style={{ color: '#94a3b8' }}>
        Fetching current operator view and read models.
      </p>
    </main>
  );
}
