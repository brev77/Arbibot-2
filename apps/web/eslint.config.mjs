import nextCoreWebVitals from 'eslint-config-next/core-web-vitals';

/** ESLint 9 flat config — Next.js 16: используйте `eslint .`, не `next lint`. */
const eslintConfig = [...nextCoreWebVitals];

export default eslintConfig;
