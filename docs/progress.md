# Журнал прогресса (append-only)

Ниже — хронологические записи: дата, задача, статус, следующие шаги. Старые строки не удалять.

| Дата | Задача | Статус | След. шаги |
|------|--------|--------|------------|
| 2026-04-12 | Синхронизация `DEVELOPMENT_PLAN.md`: Phase 1 foundation (§1.1 + §1.2) → `done`; §1.3 (`P1-1.3-NEXT` / `LAYOUT` / `M1` / `STUBS`) → `review_passed`; матрица `PRIO-P0-{CANON,INTAKE,OPP,RISK,CAP,OIB,AUD}` → `done` по канонам; блок «Последнее обновление» обновлён | Зафиксировано в плане | Закрыть §1.3 в `done` после явного DoD §50.3 и ревью operator UI; вести `PRIO-P0-EPL` / `P2-2.1-EPL` до согласованного scope; при необходимости полный прогон `npm run lint` / `build` / `test` и graphify refresh |
| 2026-04-16 | Phase 2.1 gate: CI job **`e2e-phase2`** (`tools/ci-e2e-phase2.sh`), `HttpVenueAdapter` + `lab-venue-stand.mjs`, `npm run ci:e2e-phase2`; шаги **`P2-2.1-*`** и **`PRIO-P0-EPL`** → **`done`** в `DEVELOPMENT_PLAN.md`; settlement doc (обязательный portfolio URL); `docs/TODO.md` (freeze после `done`, удалены устаревшие P0-0.3/CI); `AGENTS.md` | Зафиксировано в коде и плане | Следующий фокус: **`P2-2.2-*`** (профили, adaptive risk, playbooks) по roadmap; первый реальный CEX/DEX адаптер вне lab HTTP; при желании — `fetchOperatorBffJson` UX |
| 2026-04-16 | Phase 2.1: hardening (BFF `/api/operator` RBAC в middleware, settlement без тихого пропуска portfolio без URL, точность decimal в portfolio), `/review-step`, шаги `P2-2.1-VEN`/`EPL`/`FILL`/`PORT`/`RECON` → **`review_passed`**, коммит `ccb95ab` и push в `origin/main` | Зафиксировано в коде и плане; на remote | Перевести в **`done`** по процессу после приёмки релиза; хвост плана: CI e2e Phase 2, live venue (`P2-2.1-VEN`); при желании улучшить сообщения об ошибках BFF в `fetchOperatorBffJson`; синхронизировать `docs/settlement-post-commit.md` с fail-fast portfolio URL |
