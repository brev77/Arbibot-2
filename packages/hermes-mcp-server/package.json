{
  "name": "@arbibot/hermes-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for Hermes Agent — translates MCP tool calls to Hermes Gateway HTTP API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node --max-old-space-size=8192 ../../node_modules/typescript/bin/tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" --config ../../eslint.config.mjs",
    "test": "jest --config jest.config.cjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "jest": "^30.4.2",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.2.0"
  }
}