{
  "name": "arbibot-2",
  "private": true,
  "packageManager": "npm@11.7.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev:risk": "npm run start:dev -w @arbibot/risk-service",
    "dev:web": "npm run dev -w @arbibot/web"
  },
  "devDependencies": {
    "turbo": "^2.9.5"
  }
}
